#include "libft.h"
#include "push_swap.h"

/*void    control_0_1(int *stack_a, int *stack_b, int *numero_minimo, int *size_a, int* size_b)
{
    int n;

    n = *size_a;
    if (*size_a <= 1)
    {
        return;
    }
    if (stack_a[0] > stack_a[1])
        sa(stack_a, size_a);
    control_minimo(stack_a,stack_b, numero_minimo, size_a, size_b);
    if (n != *size_a)
        control_0_1(stack_a, stack_b, numero_minimo, size_a, size_b);
}*/