#include "libft.h"
/*void	control_minimo(int *stack_a, int *stack_b, int *numero_minimo, int *size_a, int *size_b)
{
    int n;

    n = *size_a;
    if (stack_a[0] == *numero_minimo)
    {
		pb(stack_a, stack_b, size_a, size_b);
        if (*size_a >= 1)
        {
            *numero_minimo = ft_numero_minimo(stack_a, n);
            control_minimo(stack_a, stack_b, numero_minimo, size_a, size_b);
        }
    }
}
void    control_posicion_minimo(int *stack_a, int *size_a, int numero_minimo)
{
    int n;
    int posicion_minimo;

    n = 0;
    posicion_minimo = 0;
    while (numero_minimo != stack_a[n])
    {
        if (n == *size_a -1)
            break;
        n++;
    }
    if (n <= *size_a / 2)
        posicion_minimo = 1;
    else
        posicion_minimo = 2;
    posicionar_minimo(stack_a, size_a, posicion_minimo, numero_minimo);
}*/


